# Pull Request Workflow

Before creating a PR, run:

```bash
go test ./...
go build -o freee .
```

Do not add changesets or npm packaging; this repository now ships Go binaries through GitHub Releases.
